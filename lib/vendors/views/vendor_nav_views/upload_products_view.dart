import 'package:flutter/material.dart';

class UploadProductsView extends StatelessWidget {
  const UploadProductsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Upload Products View'),
      ),
    );
  }
}
