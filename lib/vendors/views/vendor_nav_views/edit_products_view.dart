import 'package:flutter/material.dart';

class EditProductsView extends StatelessWidget {
  const EditProductsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Edit Products View'),
      ),
    );
  }
}
